# LLM配置文件
# 注意：不要在版本控制系统中存储真实的API密钥

llm:
  api_key: ${EASE_LLM_API_KEY}
  base_url: "https://api.pumpkinaigc.online/v1"
  model: "gpt-4o"
  temperature: 0.7
  max_tokens: 512

# 备用配置（如需使用官方OpenAI）
# openai_official:
#   api_key: ${OPENAI_API_KEY}
#   base_url: "https://api.openai.com/v1"
#   model: "gpt-4o"
